{
  "name": "gemverse",
  "version": "2.0.0",
  "description": "Gemverse Chat Application",
  "repository": {
    "type": "git",
    "url": "https://github.com/Karol-Witkowski/Gemverse"
  },
  "scripts": {
    "dev": "concurrently \"npm run serve --prefix client\" \"npm run dev:watch --prefix server\" ",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^6.2.0"
  }
}
